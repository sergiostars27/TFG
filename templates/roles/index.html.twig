{% extends 'baseGame.html.twig' %}

{% block title %}Hello RolesController!{% endblock %}

{% block body %}
    {{ parent() }}

        {% for form in forms %}
            <div class="container p-3 my-4">
                <div class="row">
                    <div class="col-md-2 col-xs-12 mx-auto text-center pt-2 border border-4 rounded border-dark" id="bg-2">
                    <a>{{users[loop.index0].user.username}}</a>
                    {{form_start(form)}}
                    
                    {{form_end(form)}}
                    </div>
                </div>
            </div>
        {% endfor %}
{% endblock %}
