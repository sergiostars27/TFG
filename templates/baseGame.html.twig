{% extends 'base.html.twig' %}

  {% block body %}
        
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/home">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="{{ path('gameDetails', {id:game.id}) }}">Zona Común</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('chat', {id:game.id}) }}">Chat</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Fichas</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_invitation', {id:game.id}) }}">Invitar</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Historial</a>
            </li>

            {% if rol %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path('roles', {id:game.id}) }}">Jugadores</a>
              </li>
            {% endif %}

          </ul>
        </div>
        <a class="nav-link text-dark" href="/logout">Cerrar sesión</a>
      </div>
    </nav>
    

        
  {% endblock %}


